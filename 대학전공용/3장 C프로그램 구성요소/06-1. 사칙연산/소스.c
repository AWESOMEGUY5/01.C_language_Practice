#include <stdio.h>

int main(void)
{
	int x = 20, y = 10, sum, diff, mul, div;
	
	printf("두 수의 합 : %d \n", sum = x + y);
	printf("두 수의 차 : %d \n", diff = x - y);
	printf("두 수의 곱 : %d \n", mul = x * y);
	printf("두 수의 몫 : %d \n", div = x / y);

	return 0;
}